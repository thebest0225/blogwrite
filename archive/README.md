# 미발행 초안 보관소

블로그라이터 `work_items` 에서 정리한 **미발행 완성글** 을 여기에 보관한다.
DB 에서는 지웠지만 내용은 손실 없이 남아있다 — 나중에 발행하거나 재활용할 수 있다.

## unpublished_20260820 — 27건

2026-08-04 ~ 08-12 사이에 생성됐지만 발행되지 않은 글. 해당 사이트는 그 뒤에도
정상 발행을 계속했으므로 파이프라인 장애가 아니라 **누락된 잔재** 다.

발행글과 제목을 대조한 결과 **중복 0건 — 27건 전부 고유 내용** 이었다.
(그래서 그냥 지우지 않고 파일로 남겼다.)

| 사이트 | 건수 |
|---|---|
| `ogu_main` 오구온라인 | 9 |
| `todaymoba` 더케이타임즈 | 9 |
| `accmro8zawv86856f` 오늘의머니바다 | 9 |

### 구조
```
unpublished_20260820/
  MANIFEST.json          ← 27건 목록 (id·사이트·제목·생성일·파일경로)
  <destination_id>/
    YYYY-MM-DD_제목.json ← work_items 행 전체 (article_json 포함)
    YYYY-MM-DD_제목.html ← 본문 HTML (브라우저로 바로 열어 확인)
```

### 안에 서로 중복인 쌍 (둘 중 하나만 쓰면 됨)
같은 주제를 두 번 생성한 것들이다. 발행할 때 골라 쓰라.
- 에너지바우처 (08-05 × 2, 오늘의머니바다)
- 노란우산공제 (08-07 × 2, 오늘의머니바다)
- 자동차세 9월 연납 (08-08 × 2, 오늘의머니바다)
- 내보험찾아줌 숨은 보험금 (08-06 × 2, 오구온라인)

### 되살리는 법
`.json` 의 `title` / `html` / `article_json` 을 그대로 써서 발행하면 된다.
DB 로 직접 복원하려면 정리 직전 백업이
`/var/backups/blogwrite/blogwrite.db.bak_20260820_cleanup` 에 있다.
